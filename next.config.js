/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com','img.freepik.com','marketplace.canva.com','images.pexels.com'],
      },
}

module.exports = nextConfig
